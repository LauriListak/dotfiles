Yet another repository for storing dotfiles.

## Installation
```
git clone git@github.com:LauriListak/dotfiles.git ~/.dotfiles
cd ~/.dotfiles
chmod u+x update
./update
# if fish was installed from PPA
chsh -s /usr/bin/fish
```
